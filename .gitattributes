# --- GitHub Linguist overrides ---
# Lang Lab is a polyglot benchmark lab: the per-language implementations under
# languages/<lang>/ ARE the project, so they DO count toward the repo's language
# stats (the language bar should show every backend the lab measures). Only
# genuinely third-party or generated files are excluded below.

# Upstream third-party: the qemu TCG insn plugin (GPL-2.0, Emilio G. Cota).
languages/_base/qemu-plugin/**  linguist-vendored

# Generated artifacts: CI result history (JSON) and the SVG charts/matrix.
results/**       linguist-generated
docs/charts/**   linguist-generated
